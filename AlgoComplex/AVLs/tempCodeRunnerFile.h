= getBiggerHeight(*T) + 1;
  // int BF = getHeight((*T)->left) - getHeight((*T)->right);

  // if (BF > 1)
  // {
  //   if (elem > (*T)->right->key)
  //     leftRotate(&(*T)->right);
  //   rightRotate(&(*T)->left);
  // }
  // else if (BF < -1)
  // {
  //   if (elem < (*T)->left->key)
  //     rightRotate(&(*T)->right);
  //   leftRotate(&(*T)->left);
  // }